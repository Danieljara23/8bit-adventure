---
import Layout from "../layouts/Layout.astro";
---


<Layout title="Step 1">
    <main>
        <div class="container">
          <h3 class="clue-text">A veces, cambiar de perspectiva (o de background ) ayuda</h3>
          <p>No muerde ni ladra, pero tiene dientes y la casa guarda. ¿Qué es?</p>
          <div class="input-container">
            <label for="riddleinput">Respuesta</label>
            <input name="riddleinput" type="text" id="riddleinput">
          </div>
        </div>
    </main>
</Layout>
<script>
   const rigthAnswers = ['llave', 'la llave', 'una llave']
   document.querySelector('#riddleinput')?.addEventListener('input', (e)=>{
    const inputValue = e.target.value.toLowerCase()

    if(rigthAnswers.includes(inputValue)) {
      setTimeout(()=>{
       window.location.href = '/step2'
      }, 2000)
    }
   })

   setTimeout(()=>{
     document.querySelector('.clue-text').classList.add('show')
   }, 180000)

</script>
<style>
    main {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
    }
    h1 {
        color: white;
        text-align: center;
        background-image: linear-gradient(
            -225deg,
            #231557 0%,
            #44107a 29%,
            #ff1361 67%,
            #fff800 100%
        );
        background-size: auto auto;
        background-clip: border-box;
        background-size: 200% auto;
        color: #fff;
        background-clip: text;
        text-fill-color: transparent;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: textclip 3s linear infinite;
        display: inline-block;
        font-size: 45px;
    }
    p {
        color: #13151a;
        font-size: 45px;
    }

    .container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .input-container {
      display: flex;
      width: 200px;
      align-items: center;
      justify-content: space-between;
      gap: 5px;
    }

    .clue-text {
      opacity: 0;
      text-align: center;
      background-image: linear-gradient(
          -225deg,
          #231557 0%,
          #44107a 29%,
          #ff1361 67%,
          #fff800 100%
      );
      background-size: auto auto;
      background-clip: border-box;
      background-size: 200% auto;
      color: #fff;
      background-clip: text;
      text-fill-color: transparent;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      animation: textclip 6s linear infinite;
      display: inline-block;
      font-size: 20px;
    }

    label {
      color: white;
      font-size: 24px;
    }

    @keyframes textclip {
        to {
            background-position: 200% center;
        }
    }

    .clue-text.show {
      opacity: 1;
      transition: opacity 0.5s ease-out;;
    }
   
</style>
